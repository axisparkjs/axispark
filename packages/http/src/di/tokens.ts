import { InjectionToken } from '@axisparkjs/di';

/**
 * An injection token for the HTTP options.
 */
export const HTTP_OPTIONS = new InjectionToken('HTTP_OPTIONS');
/**
 * An injection token for the HTTP adapter.
 */
export const HTTP_ADAPTER = new InjectionToken('HTTP_ADAPTER');
/**
 * An injection token for the HTTP logger.
 */
export const HTTP_LOGGER = new InjectionToken('HTTP_LOGGER');
