/**
 * Enumeration of HTTP methods used in requests.
 */
export enum HttpMethod {
    Get = 'get',
    Post = 'post',
    Put = 'put',
    Patch = 'patch',
    Delete = 'delete',
    Options = 'options',
    Head = 'head'
}
