export enum HttpParameter {
    REQUEST = 'request',
    RESPONSE = 'response',
    BODY = 'body',
    PARAM = 'param',
    QUERY = 'query',
    HEADER = 'header',
    IP = 'ip',
    SESSION = 'session'
}
